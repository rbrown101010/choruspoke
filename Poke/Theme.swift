import SwiftUI

enum PokeTheme {
    // Background - very dark grey gradient, no blue
    static let backgroundTop = Color(red: 0.09, green: 0.09, blue: 0.09)
    static let backgroundBottom = Color(red: 0.14, green: 0.14, blue: 0.14)
    
    static let cardBackground = Color("CardBackground")
    static let cardBorder = Color("CardBorder")
    static let primaryText = Color("PrimaryText")
    static let secondaryText = Color("SecondaryText")
    static let tertiaryText = Color("TertiaryText")
    static let iconColor = Color("IconColor")
    static let accent = Color("Accent")
    static let inputBackground = Color("InputBackground")
    static let separator = Color("Separator")
    static let background = Color("Background")
    
    static let cardRadius: CGFloat = 16
}
